#include<iostream>
using namespace std;

int choose(int n, int r) {
   if (n == 0)
      return 1;
   else if (n == r)
      return 1; 
   else
      return choose(n-1, r-1) + choose(n-1, r);
}

int main() {

   int n, r, result;
   n = 3;
   r = 5;
   result = choose(n, r);
   cout << result << endl;

   system("pause");
   return 0;
}














